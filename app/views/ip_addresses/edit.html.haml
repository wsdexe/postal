- page_title << "IP Pools"
- page_title << @ip_pool.name
- edit_title = @ip_pool.proxy? ? "Edit proxy address" : "Edit IP address"
- page_title << edit_title
.pageHeader
  %h1.pageHeader__title
    %span.pageHeader__titlePrevious
      = link_to "IP Pools", :ip_pools
      &rarr;
      = @ip_pool.name
      &rarr;
      Edit
      &rarr;
    = edit_title

.pageContent.pageContent--compact
  = render 'form'

